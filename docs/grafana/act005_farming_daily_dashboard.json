{
  "title": "Farming Daily",
  "uid": "act005-farming-daily",
  "tags": ["farming", "act-005"],
  "timezone": "utc",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1h",
  "time": { "from": "now-30d", "to": "now" },
  "templating": {
    "list": [
      {
        "name": "DS_POSTGRES",
        "type": "datasource",
        "query": "postgres",
        "label": "Postgres S1",
        "current": {}
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "title": "Reward / Fees / Net по дням (+ 7д MA)",
      "type": "timeseries",
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "postgres", "uid": "${DS_POSTGRES}" },
      "fieldConfig": {
        "defaults": { "custom": { "drawStyle": "bars", "fillOpacity": 30 }, "unit": "currencyUSD" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "net_usd_ma7" }, "properties": [ { "id": "custom.drawStyle", "value": "line" }, { "id": "custom.lineWidth", "value": 3 } ] },
          { "matcher": { "id": "byName", "options": "reward_usd_ma7" }, "properties": [ { "id": "custom.drawStyle", "value": "line" }, { "id": "custom.lineWidth", "value": 2 } ] }
        ]
      },
      "targets": [
        {
          "refId": "A",
          "format": "time_series",
          "rawSql": "WITH daily AS (\n    SELECT snap_date,\n           sum(reward_usd) AS reward_usd,\n           sum(fees_usd)   AS fees_usd,\n           sum(reward_usd) - sum(fees_usd) AS net_usd\n    FROM farming_daily_snapshot\n    WHERE snap_date BETWEEN $__timeFrom()::date AND $__timeTo()::date\n    GROUP BY snap_date\n)\nSELECT\n    snap_date AS time,\n    reward_usd, fees_usd, net_usd,\n    AVG(net_usd)    OVER (ORDER BY snap_date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS net_usd_ma7,\n    AVG(reward_usd) OVER (ORDER BY snap_date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS reward_usd_ma7\nFROM daily\nORDER BY snap_date;"
        }
      ]
    },
    {
      "id": 2,
      "title": "Capital по дням + net/capital %",
      "type": "timeseries",
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 9 },
      "datasource": { "type": "postgres", "uid": "${DS_POSTGRES}" },
      "fieldConfig": {
        "defaults": { "unit": "currencyUSD" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "net_pct_of_capital" }, "properties": [ { "id": "unit", "value": "percent" }, { "id": "custom.axisPlacement", "value": "right" } ] }
        ]
      },
      "targets": [
        {
          "refId": "A",
          "format": "time_series",
          "rawSql": "SELECT\n    snap_date AS time,\n    sum(capital_usd) AS capital_usd,\n    sum(reward_usd) - sum(fees_usd) AS net_usd,\n    ROUND(\n        100.0 * (sum(reward_usd) - sum(fees_usd)) / NULLIF(sum(capital_usd), 0)\n    , 4) AS net_pct_of_capital\nFROM farming_daily_snapshot\nWHERE snap_date BETWEEN $__timeFrom()::date AND $__timeTo()::date\nGROUP BY snap_date\nORDER BY snap_date;"
        }
      ]
    },
    {
      "id": 3,
      "title": "Reward по рынкам (stacked)",
      "type": "timeseries",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 18 },
      "datasource": { "type": "postgres", "uid": "${DS_POSTGRES}" },
      "fieldConfig": {
        "defaults": { "custom": { "drawStyle": "bars", "stacking": { "mode": "normal" }, "fillOpacity": 70 }, "unit": "currencyUSD" }
      },
      "targets": [
        {
          "refId": "A",
          "format": "time_series",
          "rawSql": "SELECT\n    fds.snap_date AS time,\n    COALESCE(fmc.slug, fds.condition_id, fds.token) AS metric,\n    sum(fds.reward_usd) AS reward_usd\nFROM farming_daily_snapshot fds\nLEFT JOIN farming_market_candidates fmc ON fmc.condition_id = fds.condition_id\nWHERE fds.snap_date BETWEEN $__timeFrom()::date AND $__timeTo()::date\nGROUP BY fds.snap_date, COALESCE(fmc.slug, fds.condition_id, fds.token)\nORDER BY fds.snap_date;"
        }
      ]
    }
  ]
}
